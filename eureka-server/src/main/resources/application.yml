server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: localhost
  client:
    # Un servidor Eureka no debe registrarse a s� mismo
    register-with-eureka: false
    fetch-registry: false
    service-url:
      # La URL a la que los **clientes** apuntar�n
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

# Opcional: Actuator m�nimo
management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  endpoint:
    health:
      show-details: never
